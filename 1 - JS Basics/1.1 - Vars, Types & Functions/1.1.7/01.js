// Crea una función que devuelva el texto proporcionado en mayúsculas
// 👇🏻 Define aquí tu función
// v.1 funcion normal
const toUppercase = function uc(input) {
  console.log(input.toUpperCase());
};
// v.2 Funcion flecha
const toUpperCase1 = (input) => {
  console.log(input.toUpperCase());
};
// 👆🏻
toUppercase('hola que tal');
toUpperCase1('me llamo Carlos');

// console.log('hola que tal'.toUpperCase());

//clase 28/11/22
const toUppercase = (text) => {
  return text.toUpperCase();
}
